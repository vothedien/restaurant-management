# Frontend

Install dependencies with `npm.cmd install`, copy `.env.example` to `.env`, and run `npm.cmd run dev` from this directory.

The app reads `VITE_API_URL` (default `http://localhost:8000`) and contains no secrets. Use `npm.cmd run lint` and `npm.cmd run build` before opening a Pull Request.
